machine:
  java:
    version: openjdk8

dependencies:
    pre:
        - echo y | android update sdk --no-ui --all --filter "platform-tools, tools"
        - echo y | android update sdk --no-ui --all --filter "android-25, build-tools-25.0.2"
        - echo y | android update sdk --no-ui --all --filter "extra-android-m2repository"
        - echo y | android update sdk --no-ui --all --filter "extra-android-support"
    override:
        - chmod +x gradlew

test:
    override:
        # Run tests and generate artifacts.
        - ./gradlew clean testDebugUnitTest assembleDevDebug
        # Copy the build artifacts to the output folder.
        - cp -r app/build/outputs $CIRCLE_ARTIFACTS
        # Copy the test results to the tests output folder.
        - cp -r app/build/test-results/* $CIRCLE_TEST_REPORTS